# ODE Project Website

An interactive React/Vite visualization for a Solow-style portfolio dynamics
ODE model.

## Local Development

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

## Deploy

This repo includes `netlify.toml`, so Netlify can deploy it with:

```text
Build command: npm run build
Publish directory: dist
```
